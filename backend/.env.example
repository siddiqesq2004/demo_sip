PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=credora_super_secret_jwt_token_key_2026
JWT_EXPIRES_IN=7d

# MySQL Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=credora_db
DB_PORT=3306

# SQLite fallback mode if MySQL server is not running locally (Set DB_MODE=mysql or DB_MODE=sqlite)
DB_MODE=sqlite
